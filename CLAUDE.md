# Claude Code 작업 지침

이 파일은 이 저장소에서 작업하는 AI 어시스턴트를 위한 것입니다.
사람이 읽어도 좋지만, 주 대상은 AI입니다.

---

## 이 프로젝트의 성격

1인이 AI와 함께 코인을 만들어가는 과정의 공개 기록입니다.
**유일한 약속은 "멈추지 않는 것"**이고, 나머지는 전부 부차적입니다.

개발자는 스마트컨트랙트 초심자입니다. 설명은 친절하게, 판단은 보수적으로 해주세요.

---

## 작업 체크리스트

**파일을 하나라도 바꾼 턴에는** 응답 맨 끝에 한 줄로 붙입니다.
질문에 답만 한 턴, 코드를 읽기만 한 턴에는 붙이지 않습니다 —
검사할 것이 없는데 ✅를 찍으면 그게 바로 형식만 갖춘 기록입니다. (D-012)

```
[체크리스트 · 마지막 발행 없음 (W00 초안)] 1범위✅ 2정합✅ 3불가역— 4안전선✅ 5검증✅ 6기록⚠️
```

| 기호 | 뜻 |
|---|---|
| ✅ | 확인했고 이상 없음 |
| ⚠️ | 확인했으나 사람 판단이 필요함 |
| ❌ | 위반을 발견함 — **작업을 멈추고 알립니다** |
| — | 해당 없음 |

`—`가 따로 있는 이유는, 문서만 고친 턴에 「불가역 ✅」라고 쓰는 것이
확인하지 않은 것을 확인했다고 말하는 일이기 때문입니다.
**이 프로젝트에서 그 습관이 가장 위험합니다.**

앞머리의 시계는 종료 조건(D-009)을 눈에 보이게 두기 위한 것입니다.
기준은 D-009 그대로 **마지막 기록 해시 트랜잭션 시각**이고,
아직 발행이 없으면 `마지막 발행 없음`으로 씁니다.
6주를 넘기면 체크리스트와 별개로 사용자에게 먼저 말을 꺼내세요.

### 여섯 항목

| | 항목 | 무엇을 보는가 |
|---|---|---|
| 1 | **범위** | 요청받은 것만 했는가. 커스텀 코드 0줄이 유지되는가. 컨트랙트에 기능이 늘지 않았는가 (D-001) |
| 2 | **정합** | **(작업 전)** `git status`가 깨끗한가. **(작업 후)** `docs/TOKENOMICS.md` ↔ `script/Deploy.s.sol` ↔ `test/` ↔ `README.md` 4자가 한 방향인가. 숫자를 바꿨으면 `test/`를 `grep` 했는가 |
| 3 | **불가역** | 배포 후 못 바꾸는 값을 건드렸는가 — 이름·심볼·decimals·총 발행량·베스팅 일정·장기 재고 `start`·타임락 지연 |
| 4 | **안전선** | 키와 `.env`를 건드리지 않았는가. 사용자 대신 `--broadcast` 하지 않았는가. 수익·상장·날짜 약속 문구가 들어가지 않았는가 |
| 5 | **검증** | 실제로 돌린 명령과 그 출력. `forge build` / `forge test` / `forge fmt --check` |
| 6 | **기록** | 남이 확인할 수 있는 항목이 남았는가. 판단은 `docs/DECISIONS.md`에 D-0NN으로, 실패는 `log/` 거리로 |

### 2번의 앞쪽 절반은 손대기 전에 합니다

**작업을 시작하기 전에 `git status`를 확인하세요.**

미커밋 변경이 있으면 다른 세션이 작업하던 중일 수 있습니다.
그 위에 덮어쓰면 남의 작업이 조용히 사라지고, 되돌리기도 어렵습니다.
**깨끗하지 않으면 손대기 전에 사용자에게 무엇이 바뀌어 있는지 먼저 알리세요.**

그리고 **남이 바꿔둔 파일을 커밋할 때는 그 파일의 diff를 전부 읽으세요.**
자기가 고친 줄만 보고 커밋하면 남이 지운 것을 대신 확정해 주게 됩니다.
실제로 그렇게 해서 이 체크리스트 자체가 한 번 삭제된 채 커밋됐습니다. (f5d7494)

작업이 끝나면 2번의 뒤쪽 절반 — 문서와 코드가 한 방향인지 — 를 확인합니다.

### 붙는 규칙

- **5번은 「통과했다」고 말하는 것이 아니라 출력을 붙입니다.**
  안 돌렸으면 `⚠️`이고 "안 돌렸음"이라고 씁니다.
  여기서 거짓말하면 나머지 다섯 개가 전부 무의미해집니다.
- **3번이 `⚠️`이면 그 턴에 확정하지 말고 사용자에게 먼저 물으세요.**
- `⚠️`·`❌`인 항목만 아래에 두세 줄로 풉니다. 전부 `✅`면 한 줄로 끝냅니다.
- 항목을 늘리거나 이름을 바꾸지 마세요. 매번 같은 여섯 개여야 빠진 게 보입니다.

### 이 체크리스트가 증명하지 않는 것

**1·2·3·4·6은 AI의 자기신고라서 그 자체로는 검증되지 않습니다.**
실제로 `README.md`의 배분표가 `docs/TOKENOMICS.md`와 어긋난 채로
여러 회차를 지났고, 그 동안 아무 경고도 울리지 않았습니다.

그러니 이 체크리스트의 역할은 **제3자에게 증명하는 것이 아니라
AI가 미끄러지는 것을 잡는 것**입니다.
증명하는 것은 커밋과 트랜잭션과 `log/`이고,
5번과 6번은 거기로 밀어주는 역할만 합니다.

**이 체크리스트를 근거로 「검증됐다」고 쓰지 마세요.**

---

## 🔴 절대 하면 안 되는 것

### 개인키를 요청하거나 다루지 않습니다

- `PRIVATE_KEY`, 니모닉, 키스토어 파일을 **읽거나 출력하거나 요청하지 마세요.**
- `.env` 파일을 읽어야 할 이유가 거의 없습니다. 필요하면 `.env.example`을 보세요.
- 사용자가 실수로 키를 붙여넣으면 **즉시 중단시키고 폐기를 권하세요.**
  이미 노출된 키는 회수가 불가능합니다.
- 배포는 **사용자 컴퓨터에서 사용자가 직접** 실행합니다. 대신 실행하지 마세요.

### 컨트랙트에 기능을 추가하지 않습니다

`src/Token.sol`은 지금 상태가 완성입니다. 다음을 **제안하지도 마세요**:

- 전송 수수료 / 세금
- 홀더 보상 / 리플렉션
- 자동 소각
- 블랙리스트 / 화이트리스트
- 일시정지 (Pausable)
- 소유자 권한 (Ownable)
- mint 함수
- 안티봇 / 최대 보유량 제한

**이유**: 감사 예산이 없습니다. 감사 비용을 없애는 유일한 정직한 방법은
감사할 커스텀 코드를 만들지 않는 것입니다.
표준 OpenZeppelin만 상속하면 감사할 것이 없습니다.

사용자가 기능 추가를 요청하면, 그것이 감사 필요를 만든다는 점과
`docs/DECISIONS.md` D-001을 상기시켜 주세요. 그래도 원하면 존중하되,
**별도 컨트랙트로 분리**하고 토큰 자체는 건드리지 않는 방향을 제안하세요.

### 타임락 운영자를 개인 지갑으로 되돌리지 않습니다

`proposers`에 EOA를 넣으면 그 키 하나가 **예약과 취소를 동시에** 쥡니다.
유출되면 도난이 아니라 **영구 동결**입니다 — 공격자도 못 가져가지만 발행자도 못 옮깁니다.
`SAFE_ADDRESS` 폴백을 되살리거나 `code.length` 검사를 빼는 제안을 하지 마세요. (D-007, D-019)

### 수익을 약속하는 문구를 쓰지 않습니다

문서, 로그, 커밋 메시지, README 어디에도:

- 수익률 / 원금 보장 / "N배" 같은 표현
- 상장 계획이나 예정 발표
- 지키지 못할 수 있는 날짜가 박힌 로드맵

**이유**: 한국에서 원금·수익 약속은 유사수신행위규제법(5년 이하 징역),
상장 예정 사기는 코인 사기 판결의 63.3%를 차지합니다.
그리고 **지키지 못한 로드맵은 사기 고의를 추단하는 근거로 쓰입니다.**

---

## 설계 원칙

### 배포 한도는 총량 비율이 아니라 풀 깊이 기준입니다

**한 회차 배포의 명목가치 ≤ 풀에 누적된 현금의 8%.**

「1년차 유통 2~3%」는 상한선일 뿐이고 실제로 물리는 건 풀 깊이입니다.
200만 개는 2% 안에 있으면서도 $2,600짜리 풀을 부숩니다. (D-006)

### 유틸리티를 제안하지 마세요

기능을 붙이는 것은 **조건 셋이 충족된 뒤에만** 검토합니다 — 연속 12주 기록,
풀 깊이 $25,000, 부담이 수요에 비례해 늘지 않는 형태. (D-017)

사용자가 유틸리티 얘기를 꺼내면 **먼저 조건이 충족됐는지 확인하고 사실대로 말하세요.**
그 다음 `docs/UTILITY.md`의 「기각한 것」 표를 함께 보세요.

특히 **기록 접근 게이팅은 영구히 기각입니다.** 「기록은 무료」를 이미 공개했습니다.
새 컨트랙트가 필요한 형태는 조건과 무관하게 감사 예산 전까지 대상이 아닙니다.

### 에어드랍을 제안하지 마세요

기본 설계에서 제외했습니다. 65,000,000은 **커뮤니티 배정이 아니라 장기 유동성 공급 재고**입니다.
이 표현을 지켜주세요 — 나눠줄 계획이 없는 물량을 배정이라 부르면 거짓말이 됩니다. (D-005)

재검토선은 풀 깊이 $25,000 근처이고, 그때도 홍보용이 아니라 기여자 보상 형태입니다.

### 체인은 Base입니다 — 이더리움 L1이 아닙니다

테스트넷 Base Sepolia(84532), 메인넷 Base(8453). **배포하면 사실상 되돌릴 수 없습니다.** (D-018)

**가스는 ETH로 냅니다. USDC로 못 냅니다.** 「단면 유동성이라 내 현금 0」은
풀 얘기지 가스 얘기가 아닙니다 — 메인넷에서는 실제 ETH가 소액 듭니다.
문서에 「내 현금이 안 들어갑니다」라고 쓸 때 이 구분을 흐리지 마세요. (D-014)

### 유동성에 사용자 현금을 넣지 않습니다

**상대 자산은 USDC입니다.** ETH도 USDT도 아닙니다 — 주간 보고 숫자의 분모가
흔들리면 안 되기 때문입니다. (D-011)

단면(single-sided) 유동성만 씁니다. 상수곱 AMM에서
**판매자가 가져갈 수 있는 현금의 상한은 내가 넣은 금액**이기 때문입니다.
2-sided 풀 생성을 제안하지 마세요.

**돈을 걷을 때** — 수수료 수령(`collect`)은 자유롭게. 판매 대금 회수(`decreaseLiquidity`)는
매수벽을 제거하므로 **금액과 이유를 기록에 먼저 쓰고** 실행합니다.

### 종료 조건이 있습니다

**8주 연속 검증 가능한 기록이 없으면 종료**이고, 판정은 마지막 기록 해시 트랜잭션 시각입니다.
이 숫자를 **늘리는 방향으로 바꾸지 마세요.** 줄이는 것만 가능합니다. (D-009, D-013)

### 공지와 코드가 다르면 안 됩니다

**`docs/TOKENOMICS.md` ↔ `script/Deploy.s.sol` ↔ `test/` ↔ `README.md` 4자를 대조하세요.**
어느 둘이라도 어긋나면 **작업을 멈추고 사용자에게 알리세요.** 사소한 불일치가 아닙니다.

이 규칙은 두 번 넓어졌습니다. 넓힐 때마다 그 틈으로 사고가 났기 때문입니다.

| 언제 | 무엇이 방치됐나 |
|---|---|
| TOKENOMICS ↔ Deploy 만 비교하던 때 | `README.md`의 배분표가 구버전(60/15/20/5)으로 여러 회차 방치 |
| 위 셋을 비교하던 때 | 초기 LP가 2M→4M 으로 바뀌었는데 `test/`가 2M을 단언한 채 **60개 전부 통과** |

**테스트는 통과한다고 맞는 것이 아닙니다.** 옛 숫자를 하드코딩한 테스트는
틀린 상태를 고정할 뿐이고, 통과하기 때문에 오히려 안 보입니다.
숫자를 바꿨으면 `test/`에서 그 숫자를 `grep` 하세요.

숫자의 단일 출처는 `TOKENOMICS.md`이고, 다른 문서는 링크만 겁니다.
`test/Deployment.t.sol`의 `test_DeployScriptUsesTheSameNumbers`가
Deploy 상수와 테스트 숫자를 묶어두지만, **문서까지 묶어주지는 않습니다.**

### 강제되는 것과 약속을 섞어 쓰지 마세요

문서에 「코드가 강제한다」고 쓰려면 **그 코드가 지금 저장소에 있어야** 합니다.
`docs/HANDOVER.md`의 예정 작업은 강제가 아닙니다. (D-014)

특히 이 셋은 **강제가 아닙니다** — 잘못 쓰지 않도록 기억하세요.

- **베스팅 수령권은 `transferOwnership`으로 매각 가능합니다** (OZ가 소스에 직접 경고).
  수령 주소를 타임락으로 지정해 7일 예약을 걸어뒀지만, 불가능해진 게 아니라 **보이게** 된 것입니다
- **타임락 `updateDelay`는 7일 예고를 거쳐 지연을 0으로 만들 수 있습니다.** admin 포기로 안 막힙니다
- **종료 시 소각·LP 미회수는 사람이 수행하는 행동입니다**

### 되돌릴 수 없는 것들

배포 후 변경 불가: **이름, 심볼, decimals, 총 발행량, 베스팅 일정.**
이 값들을 바꾸는 작업을 할 때는 반드시 되돌릴 수 없다는 점을 먼저 알리세요.

---

## 자주 하는 작업

```bash
forge build          # 컴파일
forge test           # 테스트 (62개 통과해야 정상)
forge test -vvv      # 실패 시 상세 출력
forge fmt            # 포맷
forge test --gas-report

# 테스트넷 배포 — 사용자가 직접 실행
forge script script/Deploy.s.sol --rpc-url base_sepolia --broadcast --verify
```

### 테스트를 고칠 때

테스트가 실패하면 **테스트를 고치기 전에 컨트랙트가 맞는지 먼저 확인**하세요.
특히 `test_NoMintFunctionExists` 류의 테스트가 실패한다면
누군가 컨트랙트에 기능을 추가했다는 뜻입니다. 그건 되돌려야 합니다.

### 한글 문자열

Solidity에서 한글이 들어간 문자열 리터럴은 `unicode"..."` 접두사가 필요합니다.
주석에는 필요 없습니다.

---

## 기록에 대해

`log/` 디렉토리의 주간 기록이 이 프로젝트의 핵심 산출물입니다.
코드보다 중요합니다.

사용자가 작업하다 막히거나 실패하면, **그것을 기록해두라고 상기시켜 주세요.**
실패 기록은 이 프로젝트에서 가장 값진 콘텐츠이고,
`log/_TEMPLATE.md`는 "안 된 것"을 맨 위에 쓰도록 되어 있습니다.

### 기록의 기준 — 검증 가능성

**모든 기록에 남이 직접 확인할 수 있는 항목이 최소 하나 들어가야 합니다.**
커밋 해시, 트랜잭션 링크, 주소, 또는 독립적으로 확인 가능한 숫자.

이게 없으면 기록이 아니라 일기입니다. 초안을 도울 때 이 항목이 비어 있으면
**채우라고 알려주세요.** "이번 주 커밋 0건"도 확인 가능한 숫자이므로 유효합니다.

감정을 빼라는 뜻이 아닙니다. **감정은 사실에 붙는 각주지 본문이 아닙니다.** (D-012)

초안을 도와주되, **발행은 사람이 합니다.** 자동 발행하지 마세요 —
"사람이 멈추지 않는다"가 이 프로젝트의 유일한 약속인데
봇이 대신하면 그 약속이 무의미해집니다.

---

## 참고 문서

- `README.md` — 프로젝트 개요, 시작 방법
- `docs/TOKENOMICS.md` — 배분과 근거 (숫자의 단일 출처)
- `docs/WALLETS.md` — 공개 지갑 주소
- `docs/UTILITY.md` — 유틸리티를 붙일 조건 (지금은 안 붙입니다)
- `docs/SECURITY.md` — 키와 서명 (컨트랙트 보안 아님)
- `docs/DECISIONS.md` — 결정 기록 (새 결정은 여기에)
- `log/` — 주간 기록
