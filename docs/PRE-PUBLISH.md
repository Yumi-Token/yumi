# 발행 전 최종 점검

> **이 문서는 홀더용이 아닙니다.** 읽을 의무가 없습니다.
> 이 문서는 발행 전 자기 점검표입니다. 체크되지 않은 항목이 보이는 것이 정상입니다.
> 사시기 전에 아셔야 할 것은 전부 [`PROMISE.md`](../PROMISE.md) 한 장에 있습니다.

**첫 기록을 발행하고 해시를 체인에 올리는 순간 D-009의 8주 시계가 시작됩니다.**
되돌릴 수 없습니다. 그 전에 이 목록을 끝까지 지나가세요.

---

## 1. 되돌릴 수 없는 것

**테스트넷에는 이미 배포됐습니다 (2026-08-23).** 아래는 두 갈래로 나뉩니다.

### 이미 고정된 것 — 테스트넷 기준

| | 값 | 어디에 |
|---|---|---|
| 이름·심볼 | `Test Token` / `TEST` | Base Sepolia에 영구 고정 (리허설) |
| 베스팅 일정 | 90일/365일, +365일/1095일 | 같음 |
| 타임락 | 7일, `admin = address(0)` | 같음 |

**테스트넷 값은 리허설입니다.** 메인넷은 새 컨트랙트라 아래를 다시 정합니다.

### 메인넷 배포 전에만 바꿀 수 있는 것

- [x] **토큰 이름·심볼 확정** — `Yumi` / `YUMI` (D-027). 배포하면 영원히 불변
- [ ] Base에 동명 토큰이 새로 생기지 않았는지 **배포 직전 재확인** (2026-08-24 기준 없음)
- [ ] **베스팅 일정** — 창업자 90일/365일, 재고 +365일/1095일 (D-020, 변경 함수 없음)
- [ ] **타임락 지연 7일**, `admin = address(0)`
- [ ] **수수료 티어 1%** — 풀 생성 시 결정, 이후 불변

> **위 넷은 메인넷 배포 시점 항목입니다.** 아직 배포 전이라 미체크가 정상입니다.

### 첫 기록 발행 후에는 조이는 방향으로만 (D-013)

> **여기부터 적용됩니다 — 첫 기록을 2026-08-26 에 발행했습니다.**
> 아래 셋은 이제 **줄이는 것만 가능**하고 늘릴 수 없습니다. 체크박스가 아니라 제약입니다.

- [ ] **종료 조건 8주** — 늘릴 수 없음
- [ ] **배포 한도 8%** — 같음
- [ ] **유틸리티 조건 셋** (D-017) — 낮출 수 없음

## 2. 저장소 공개 (완료) — 공개 후 점검

- [x] `.env`가 **한 번도 커밋된 적 없는지** — `git log --all --full-history -- .env`
      (결과가 비어 있어야 합니다. 나오면 그 키는 즉시 폐기하고 새로 만드세요.
       히스토리에서 지워도 이미 노출된 것으로 간주합니다)
- [x] `git log -p --all | grep -i "0x[0-9a-f]\{64\}"` — 64자리 16진수가 커밋에 없는지
- [x] `.gitignore`에 `.env`, `cache/`, `out/`, `broadcast/**/dry-run/`
      — `broadcast/`를 통째로 빼지 않습니다. 배포 트랜잭션 기록은 **일부러 커밋합니다**
        (주소와 tx 해시는 공개해도 되는 정보이고, 기록이 곧 이 프로젝트의 자산입니다).
        dry-run 결과만 제외합니다 — `.gitignore` 주석에 같은 이유가 적혀 있습니다
- [x] 공개하면 **과거 커밋 전부**가 같이 공개된다는 것 — 초기 시행착오까지
- [x] **의존성이 위변조되지 않았는가** — `git submodule status`에 `+`가 없고
      커밋 해시가 공식 태그와 같은가 ([`SECURITY.md`](SECURITY.md) 「저장소와 의존성이 바뀌지 않았는지」)

- [x] `git log --format='%an <%ae>' --all | sort -u` — **커밋 작성자 이메일이 개인 이메일인가**
      개인 이메일이면 이미 공개된 것으로 간주. GitHub 설정의 noreply 주소로 바꾸되
      **과거 커밋은 리라이트해도 캐시에 남습니다**
- [x] Issues·Discussions — 첫 기록 전까지는 **끄는 것을 권합니다** (혼자 대응해야 함)
- [x] Branch protection — 없으면 `main` 강제 푸시로 **앵커된 파일이 바뀌어 해시가 깨집니다**
- [x] Private vulnerability reporting **켜짐** (루트 `SECURITY.md` 가 그 버튼을 안내합니다)

> **2026-08-23 1차 검사 통과** — `.env` 커밋 이력 0건, 개인 경로·이메일 노출 없음,
> 추적 중인 민감 파일은 `.env.example`(빈 템플릿)뿐, 의존성 두 개 모두 공식 태그와 일치.

## 3. 문서 정합 — 4자 대조

- [x] `docs/TOKENOMICS.md` ↔ `script/Deploy.s.sol` ↔ `test/` ↔ `README.md`
- [x] 「코드가 강제한다」고 쓴 것이 **전부 저장소에 있는가** (D-014)
- [x] 강제되는 것과 약속이 문서에서 **분리돼 있는가**
- [x] 수익률·원금·「N배」·날짜 박힌 로드맵이 **0건인가**
- [x] **정해지지 않은 것을 정해진 것처럼 쓴 곳이 없는가** — 「지금 취소되면 약속을 어긴 게 되는가?」 (D-031)
- [x] 「소각」과 「소각 주소 이전」이 구분돼 있는가 (D-021)
- [x] **기록 파일 안의 `<!-- 발행 전 ... -->` 메모를 지웠는가** — 발행 후에는
      파일을 못 고칩니다(해시가 깨집니다). 메모째로 해시가 박힙니다
- [x] `README.md`의 영문 요약이 한국어 본문과 어긋나지 않는가
- [x] **`PROMISE.md`가 한 화면을 넘지 않는가** — 3,000자 이하 (D-032)
      ⚠️ **같은 파일이 셋 다르게 나옵니다.** 반드시 아래 명령을 그대로 쓰세요.
      ```powershell
      (Get-Content PROMISE.md -Raw -Encoding UTF8).Length     # 2,555  ← 이 값
      ```
      `-Encoding UTF8` 을 빼면 **3,142**, Git Bash `wc -m` 은 **5,265**(바이트)로 나옵니다.
      둘 다 3,000을 넘어서 **멀쩡한 문서를 초과로 오판하게 됩니다.** 실제로 한 번 그랬습니다
- [x] `PROMISE.md`에서 뺀 내용이 **전부 2층 어딘가에 실제로 있는가** — 하나라도 없으면 분실
- [x] `scripts/weekly.conf`의 주소가 `docs/WALLETS.md`와 일치하는가
      — 메인넷 배포 후 `NETWORK`·`RPC_URL`·`EXPLORER`를 바꿨는지 특히 확인
      (`ANCHOR_*` 는 이미 메인넷입니다. 데이터망과 별개라 같이 바꿀 필요 없습니다)
- [x] ★ **기록에 남이 직접 확인할 수 있는 항목이 하나라도 있는가** (D-012)
      커밋 해시 · 트랜잭션 링크 · 주소 · 독립 확인 가능한 숫자.
      없으면 기록이 아니라 일기이고, 종료 조건도 이 기준으로 셉니다
- [x] 🔴 **앵커를 보낼 네트워크가 Base 메인넷인가** — `ANCHOR_RPC_URL` 로 분리했습니다
      데이터는 토큰이 있는 곳(`RPC_URL`)에서 읽고, **앵커는 언제나 메인넷**입니다.
      토큰이 메인넷에 없어도 상관없습니다 — 앵커는 해시만 싣는 것이라 토큰과 무관합니다.
      테스트넷이면 스크립트가 경고합니다

## 3-1. ⚠️ 공개 순서 — LP 계획

- [ ] **메인넷 배포와 풀 생성을 같은 날 붙일 것.**
      `docs/TOKENOMICS.md`에 초기 가격 구간이 공개돼 있는데 풀이 없으면,
      제3자가 **임의 가격으로 풀을 먼저 만들어** 그리핑할 수 있습니다
      (`HANDOVER.md` 「풀 생성 전에 알아야 할 것」 ③)
- [x] 테스트넷 단계에서는 해당 없음 — 메인넷 토큰 자체가 없습니다

---

## 4. 주소 공개

- [x] 배포 4주소 + Safe + **주간 해시 지갑**이 `WALLETS.md`에 기재 — 2026-08-23 완료
- [x] Basescan 소스 검증 4/4 — 완료
- [x] 두 베스팅의 `owner()` == 타임락 — 온체인 확인 완료
- [x] `hasRole(PROPOSER_ROLE, 배포지갑)` == **false** — 확인 완료
- [x] `hasRole(EXECUTOR_ROLE, address(0))` == **true** — 확인 완료
- [x] **이름·심볼 온체인 선점 선언 완료** — 2026-08-26 14:30:29 UTC, `0x7c7ae8a5…a6e6` (D-035)
- [ ] Base 메인넷에 `YUMI` 심볼의 다른 토큰이 새로 생기지 않았는지 **배포 직전 재확인**

## 5. 기록 루틴이 실제로 도는지

**여기가 제일 중요합니다.** 8주 시계는 코드가 아니라 습관이 지킵니다.

- [x] 해시 기록 트랜잭션을 **테스트넷에서 최소 1회 리허설** — 2026-08-23
- [x] 그 절차가 **10분 안에** 끝나는가 — 안 끝나면 8주를 못 버팁니다
- [x] 해시 지갑에 가스비가 들어 있는가 — 0.001 ETH
- [x] 판정 기준 시각이 문서에 명시돼 있는가 — `log/ANCHORS.md` 「지금 시계」
- [x] 배포 채널(쓰레드) 계정 준비 — `@yumitoken`

## 6. 발행 직전에 링크에서 다시 볼 것

- [ ] **APYWA 숫자** (국내산 132개 / 해외 639개 / 79.66점 / 95.08점) — 2026-08-17 기준입니다.
      재평가로 바뀌므로 **발행 당일** 원본 링크에서 다시 확인하세요. (D-012)
- [ ] 「71개가 C 이하 등급」의 근거가 그대로인가

> **기록 #1(2026-08-26 발행)에서는 이 항목이 확인되지 않은 채 지나갔습니다.**
> 다만 본문이 **「2026년 8월 17일 평가 기준」이라고 기준일을 명시**하고 있어
> 그 시점 값으로서는 정확합니다. 나중에 재평가로 바뀌어도 거짓이 되지 않습니다.
>
> **기준일을 함께 적는 것이 이 항목의 진짜 방어입니다.** 다음 기록에서도 그렇게 쓰세요 —
> 앵커된 뒤에는 고칠 수 없으므로, 「지금 값」이 아니라 「언제 기준의 값」으로 적습니다.

---

## 7. 마지막 질문 셋

- [x] 이 기록에 **남이 직접 확인할 수 있는 항목**이 하나라도 있는가 (D-012)
- [ ] **6개월 뒤에 읽어도 부끄럽지 않은가**
- [x] 지금 그만두면 홀더가 잃는 것이 있는가 — **2026-08-26 발행 시점에는 없었습니다.**
      아직 메인넷 토큰이 없어 아무도 살 수 없었기 때문입니다.
      🔴 **풀을 만드는 순간 이 질문의 답이 영원히 「있다」로 바뀝니다.**
