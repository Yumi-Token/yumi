# 인수인계 — 다음에 할 일

이 문서는 **Claude Code가 이어서 작업할 때 읽는 지시서**입니다.

작업 전에 `CLAUDE.md`와 `docs/DECISIONS.md`를 먼저 읽으세요.
특히 `CLAUDE.md`의 **작업 체크리스트 6항목**은 파일을 고칠 때마다 적용됩니다.

---

## 현재 상태

| | |
|---|---|
| 컴파일 | ✅ solc 0.8.28 (forge 1.5.1) |
| 테스트 | ✅ **64개 통과** (Token 15 / FounderVesting 13 / Inventory 13 / Deployment 23) |
| 배포 스크립트 | ✅ 로컬 시뮬레이션 end-to-end 확인 |
| 실측 스크립트 가스 | 3,182,788 |
| 커스텀 코드 | **0줄** — 전부 OpenZeppelin 상속 또는 직접 사용 |
| 테스트넷 배포 | ✅ **2026-08-23 완료** — [`WALLETS.md`](WALLETS.md) 참조. 소스 검증 4/4 |

`src/Token.sol`과 `src/FounderVesting.sol`은 **완성입니다. 건드리지 마세요.**

---

## 코드로 구현이 끝난 것

새 컨트랙트 파일은 만들지 않았습니다. 장기 재고와 타임락은
OpenZeppelin 구체 클래스를 배포 스크립트에서 그대로 `new` 합니다.

| 버킷 | 수량 | 무엇으로 | 잠금 |
|---|---|---|---|
| 창업자 | 20,000,000 | `FounderVesting` | 3개월 클리프 + 12개월 선형 |
| 장기 재고 | 65,000,000 | `VestingWallet` | `start` = 배포 +365일 / `duration` = 1095일 |
| 운영·예비 | 10,000,000 | `TimelockController` | `minDelay` = 7일 |
| 근거리 재고 | 5,000,000 | 트레저리 잔류 | 없음 |

**두 베스팅의 수령 주소는 타임락입니다.** (D-015)

```
0. Safe 2-of-3 생성 (app.safe.global, Base)   ← 신규. 주소 확보 (D-019)
1. TimelockController      proposers = [Safe 주소]
                           executors = [address(0)]
                           admin     = address(0)
2. Token                   전량이 배포 지갑으로
3. FounderVesting          beneficiary = 타임락
4. VestingWallet (재고)     beneficiary = 타임락, start = +365일
5. 물량 이체 (20M / 65M / 10M)
   ─ 1~5는 한 broadcast ─
6. LP 5구간 생성 (4M)       배포 지갑에서
7. LP 포지션 NFT → 타임락
8. 남은 1M → Safe
9. 배포 지갑 잔고 0 확인
```

**0번이 1번보다 먼저여야 합니다.** Safe 주소가 타임락 생성자 인자입니다.
순서를 틀리면 재배포해야 하고, 그러면 이름·심볼을 태웁니다.

타임락 구성 — `proposers` = [**Safe 2-of-3**], `executors` = [`address(0)`], `admin` = `address(0)`.
실행을 열어둔 것은 키 분실 시 물량이 영구히 잠기는 것을 막기 위해서이고,
예약은 여전히 Safe만 할 수 있으므로 7일 지연은 그대로 강제됩니다.

### Safe 2-of-3 만들기 — 배포 전에

**Safe는 지갑이 아니라 컨트랙트입니다.** 키를 보관하지 않고 **주소 목록과 정족수**만 저장합니다.
그래서 **Safe 자체의 시드 문구는 없고, 서명자 키 3개가 곧 백업**입니다.

**1) 서명자 지갑 3개 준비**

| | 예시 | 조건 |
|---|---|---|
| ① | PC 메타마스크 또는 Foundry 키스토어 | 평소 사용 |
| ② | 폰 지갑 앱 | **반드시 다른 기기** |
| ③ | 종이에 적어 금고 | 오프라인. 평소 안 씀 |

각각의 **주소**만 있으면 됩니다.

**2) Safe 생성**

`app.safe.global` → **Base 네트워크** 선택 → ①번 지갑 연결
→ 주소 3개 입력, 정족수 **2** → 생성 (가스 몇 센트)

**3) 나온 Safe 주소를 `.env`의 `SAFE_ADDRESS`에 넣습니다.**

**테스트넷에서도 Safe를 만드세요.** 배포 스크립트가 EOA를 거부합니다. 메인넷에서 처음 써보면 늦습니다.

### 쓰는 법

```
①번 지갑으로 접속 → 트랜잭션 제안 + 서명   (1/2)
②번 지갑으로 접속 → 승인 + 서명           (2/2) → 자동 실행
```

두 번째 서명이 들어가는 순간 실행됩니다. 별도 실행 버튼이 없습니다.
③번은 ①이나 ②를 잃거나 털렸을 때만 꺼냅니다.

### ⚠️ 주의

- **`app.safe.global`을 북마크하고 검색으로 들어가지 마세요.** 가짜 사이트가 광고로 뜹니다
- **서명할 때 내용을 읽으세요.** 2-of-3이어도 두 번 다 잘못 승인하면 통과합니다
- **③번 키를 편하다고 PC나 클라우드에 두지 마세요.** 그 순간 2-of-3이 사실상 1-of-1이 됩니다

### 키 관리

Foundry 공식 권고를 따릅니다 — **테스트넷은 암호화 키스토어, 메인넷은 하드웨어(권장).**

```bash
cast wallet import deployer --interactive
forge script script/Deploy.s.sol --account deployer --rpc-url base_sepolia --broadcast --verify
```

**그리고 `.env`에서 `PRIVATE_KEY` 줄을 비웁니다.** 키스토어를 만들어놓고 평문을 남기면 의미가 없습니다.

**하드웨어 지갑은 「권장」이지 「필수」가 아닙니다.** Safe 2-of-3이 들어가면 단일 키 유출 손실이 0이 되므로 한계 효용이 낮습니다. 유일하게 남는 단일 키 구간은 아래 **배포 순간**이고, 그것도 일회용 새 키로 상당 부분 완화됩니다. 하드웨어는 **"내 컴퓨터가 이미 감염됐을 수도 있다"에 대한 보험**으로 평가하면 됩니다.

**⚠️ 가장 위험한 시간 — 배포하는 몇 분**

`forge script --broadcast`는 트랜잭션을 **순차 전송**하므로, 토큰 배포 직후 ~ 이체 완료 전까지 **배포 지갑에 1억 개 전량이 있는 순간이 실재합니다.**

> **완화책: 배포 키를 그 배포만을 위해 새로 만들고, 끝나면 비우고 폐기합니다.**

**⚠️ Windows 주의** — WSL2는 USB를 기본 지원하지 않아 하드웨어 지갑이 안 잡힙니다(`usbipd-win` 필요). 하드웨어를 쓸 거면 네이티브 Windows가 편합니다. `--ledger` 실패 사례가 Foundry 이슈로 다수 보고돼 있으니 **반드시 테스트넷에서 먼저 성공시키세요.**

### 지갑을 용도별로 나눕니다

| 용도 | 지갑 | 빈도 |
|---|---|---|
| 배포·LP 설치 | 배포 지갑 (일회용, 끝나면 비움) | 1회 |
| 타임락 예약·실행 | **Safe 2-of-3** | 분기 1회 |
| 근거리 재고 1M 보관 | **Safe 2-of-3** | 필요시 |
| **주간 기록 해시** | **소액 핫월렛** | **매주** |

주간 해시는 가스비만 필요하고 털려도 잃을 게 없습니다. 매주 Safe를 꺼내는 건 지속되지 않으므로 별도 소액 지갑을 씁니다. **그 주소도 `WALLETS.md`에 공개합니다** — 종료 조건 판정 근거라 어차피 공개돼야 합니다.

⚠️ **근거리 재고 1M을 Safe에 두는 것은 「잠금」이 아닙니다.** Safe는 서명 2개면 즉시 움직이고 타임락을 거치지 않습니다. **「예고 없이 움직일 수 있는 양 1%」는 그대로입니다.** 문서에 이 1M이 7일 예약 대상인 것처럼 쓰지 마세요. (D-014)

### 테스트가 고정하는 주장

| 테스트 | 고정하는 것 |
|---|---|
| `test_BothVestingBeneficiariesAreTimelock` | 해제분이 지갑으로 직행하지 않음 (D-015) |
| `test_NothingReachesDeployerDirectlyAfterTenYears` | 10년 뒤에도 86%가 되지 않음 |
| `test_NothingUnlocksAtOnceWhenVestingStarts` | 재고에 한 번에 열리는 지점 없음 (D-008) |
| `test_DeployScriptUsesTheSameNumbers` | **배포 스크립트 상수 ↔ 테스트 숫자** |
| `test_NoExternalAdmin` / `test_ExecutionIsOpenToAnyone` | 타임락 권한 구성 |

`test_DeployScriptUsesTheSameNumbers`가 유일하게 **실제 배포되는 값**과
연결된 지점입니다. 이게 없으면 `Deploy.s.sol` 상수만 바꿔도 전부 통과합니다.
그래서 `Deploy.s.sol`의 배분·일정 상수는 `public`입니다. `internal`로 되돌리지 마세요.

---

## 해야 할 일 — 사용자가 직접

키를 다루기 때문에 **AI가 대신 할 수 없습니다.**

### 1. ~~테스트넷 배포~~ ✅ 2026-08-23 완료

주소 네 개와 온체인 확인 결과는 [`WALLETS.md`](WALLETS.md)에 있습니다.
Safe(`0xeEE81a78…86aA`), 소스 검증 4/4, D-007·D-015·D-019 전부 체인에서 확인했습니다.

**남은 것은 아래 4번(0주차 발행)입니다.**

### 2. 네 주소를 `docs/WALLETS.md`에 기입 → Basescan 검증 4건

### 3. 배포 직후 온체인 확인

`START-HERE.md` 7번에 `cast` 명령이 전부 있습니다. **가장 중요한 것은 이 둘입니다:**

- 두 베스팅의 `owner()` == 타임락 주소 (여기가 지갑이면 D-015가 무효입니다)
- `hasRole(EXECUTOR_ROLE, address(0))` == true (키 분실 시 브릭 방지)

### 종료 판정이 났을 때 (실행 순서)

**한 번에 예약하세요.** `scheduleBatch`로 묶으면 7일 한 번으로 끝납니다.

1. `founderVesting.transferOwnership(0x…dEaD)`
2. `inventoryVesting.transferOwnership(0x…dEaD)`
3. 타임락 보유 토큰 `burn()`
4. LP 포지션 NFT를 소각 주소로 (⚠️ `decreaseLiquidity` 하지 말 것 — D-010)
5. Safe 보유 토큰 `burn()` — 타임락 밖이라 별도

**1·2번을 먼저 넣으세요.** 미해제 물량이 절대다수이고, 그것만이 되돌릴 수 없는 조치입니다.

### 4. `PROMISE.md` 공개 → `log/2026-W34.md`(기록 #1) 발행 → 해시 앵커

**순서가 있습니다.** (D-026)

1. **`PROMISE.md` 공개** — 약속 문서입니다. **시계를 시작시키지 않습니다**
2. **`log/2026-W34.md`(기록 #1) 발행** — 가스 실측치·테스트 개수·커밋 해시가 들어 있습니다
3. **해시 앵커** — 절차는 [`RECORD.md`](RECORD.md). **여기서 8주 시계가 시작됩니다**

**발행 전에 [`PRE-PUBLISH.md`](PRE-PUBLISH.md)를 끝까지 지나가세요.**
되돌릴 수 없습니다.

**발행은 사람이 합니다.** 자동 발행하지 마세요.
발행 전에 서두의 APYWA 숫자를 링크에서 다시 확인하세요.

---

## 그 다음에 할 만한 것

우선순위 순입니다. 그 주에 할 수 있는 것을 고르세요.

1. **주간 기록 루틴 안착** — 2주차, 3주차를 실제로 써보는 것이 코드보다 중요합니다.
2. **유동성 투입 절차 문서화** — 실행은 한참 뒤지만, 아래 ①~④를 반영한
   체크리스트를 미리 만들어두면 그때 흔들리지 않습니다.
3. ~~이름·심볼 확정~~ ✅ `Yumi` / `YUMI` (D-027)

### 풀 생성 시점에 함께 할 것 — 「사는 방법」 문서

**지금 만들지 마세요.** 풀도 메인넷 토큰도 없는데 구매 안내를 쓰면
「아직 없는 것을 안내하는 문서」가 됩니다. **풀을 만든 직후가 적기입니다.**

**그때 반드시 함께 정정할 것 — 국내 구매 경로 서술이 부정확합니다.**

현재 `docs/DECISIONS.md`(D-011, D-018)와 `docs/TOKENOMICS.md`에
「국내 구매자의 실제 경로는 원화 → ETH → Base」라고 적혀 있는데,
**업비트·빗썸은 ETH를 이더리움 네트워크로만 출금합니다.** 브릿지가 반드시 낍니다.

| 경로 | 실제 |
|---|---|
| 원화 → ETH → 이더리움 출금 → [bridge.base.org](https://bridge.base.org) → Base | **이것뿐입니다.** L1 브릿지 가스가 듭니다 |
| 원화 → AERO → Base 출금 | ⚠️ **함정** — Base 가스는 ETH로 냅니다. AERO만 들고 오면 스왑조차 못 합니다 |

**이건 Base만의 문제가 아닙니다.** Arbitrum·Optimism도 같습니다.
국내 거래소가 L2 출금을 폭넓게 지원하지 않는 것이 원인이라 **체인을 바꿔도 안 풀립니다.**
그러니 D-018의 Base 선택은 유효하고, **④번 근거 문장만 정정 대상**입니다.

**「사는 방법」 문서에 들어갈 것**

- 브릿지 단계를 **빼먹지 말 것** — 이게 실제 마찰이고 숨기면 D-014 위반입니다
- 가스로 쓸 ETH를 먼저 확보해야 한다는 것
- **주소로 확인하라**는 안내 (동명 토큰 — D-027)
- 수익을 약속하는 문구는 넣지 않습니다 (🔴)

### `docs/UTILITY.md`는 작성됐습니다 (D-017)

**유틸리티는 붙이지 않되, 붙일 조건을 먼저 공개하는** 방향으로 정했습니다.
조건 셋이 전부 충족되기 전에는 검토조차 하지 않습니다.

1. 첫 기록 발행 이후 **연속 12주** 무결
2. 풀 깊이 **$25,000** 이상 (D-005 재검토선과 같은 숫자)
3. **부담이 수요에 비례해 늘지 않는 형태** — "요청 100건이 와도 내가 하는 일이 늘지 않는가"

사용자가 유틸리티 얘기를 꺼내면 **먼저 이 조건이 충족됐는지 확인하세요.**
안 됐으면 그 사실을 말하고, 됐으면 `docs/UTILITY.md`의 「기각한 것」 표를 함께 보세요.

새 컨트랙트가 필요한 형태는 조건과 무관하게 **감사 예산이 생기기 전까지 대상이 아닙니다.** (D-001)

---

## 초기 LP 투입 — 무엇을 얼마씩 (D-016)

**숫자의 단일 출처는 [`TOKENOMICS.md`](TOKENOMICS.md) 「초기 구간 배치」입니다.**
여기에 표를 복사해두면 한쪽만 고쳐질 때 어긋납니다. 요약과 링크만 둡니다.

| 항목 | 값 |
|---|---|
| 수수료 티어 | **1%** — 풀 생성 시 정해지고 **이후 변경 불가** |
| 상대 자산 | **USDC** (D-011). 단면(single-sided), 내 현금 0 |
| 투입량 | 근거리 재고 5,000,000 중 **4,000,000** |
| 구간 | **5개**, $0.0005 ~ $0.005, 경계가 전부 맞물림 |
| 수량 배분 | 위로 갈수록 많게 (711,111 → 888,889) |
| 전량 소진 시 유입 | 약 **$8,113** |
| 투입 후 남는 근거리 재고 | **1,000,000** = 총량의 1% |

**구간별 하한·상한·수량은 [`TOKENOMICS.md` 「초기 구간 배치」](TOKENOMICS.md)에서 그대로 읽으세요.**
실제로 LP를 깔 때는 그 표를 열어놓고 하시면 됩니다.

투입량 4M과 구간 5개의 근거는 D-016에 있습니다. 요점만 —
**초기 진입자 배수를 정하는 것은 곡선 모양이 아니라 깊이**여서, 2M을 5구간으로
나누기만 하면 배수가 거의 안 줄고(3.37→3.23) 첫 매수 체결만 나빠집니다.
투입량을 두 배로 해야 배수가 실제로 눌립니다(2.57배). 구간을 나누는 것은
배수 억제가 아니라 **조달액**을 위해서입니다.

---

## ⚠️ 풀 생성 전에 반드시 알아야 할 것 넷

**① 목표 가격을 정확히 설정할 수 없습니다.** 틱 스페이싱 때문입니다. 1% 티어(spacing 200) 기준 —

| 목표 | 실제 설정되는 값 | 오차 |
|---|---|---|
| $0.0005 | $0.00049685 | −0.63% |
| $0.005 | $0.00495513 | −0.90% |
| $0.05 | $0.04941768 | −1.17% |

**문서의 $0.0005를 그대로 공지하면 「공지와 코드가 다른」 상태가 됩니다.**
풀 생성 후 **실제 tick에서 역산한 가격**을 공개 숫자로 확정하고 문서를 고치세요.

**② 토큰이 token0일지 token1일지 배포 전에 알 수 없습니다.** 주소 크기로 정해지고 사실상 난수입니다.
방향이 뒤집히면 "현재가 위쪽에 구간을 잡는다"의 부호가 반대가 되고, **틀리면 mint 시 USDC를
요구해서 「내 현금 0」 전제가 깨집니다.**

- TOKEN이 token0 → `현재 tick < tickLower`
- TOKEN이 token1 → `현재 tick > tickUpper`

**③ 풀을 직접 초기화하세요.** `createAndInitializePoolIfNecessary`로 초기화하고 **즉시** mint하지 않으면
제3자가 임의 가격으로 먼저 초기화할 수 있습니다.

**④ Uniswap v3를 쓰세요.** v4에는 `collect()`가 없고 수수료 회수와 원금 회수가 같은
함수(`modifyLiquidities`)를 공유합니다. 그러면 "무엇을 회수했는지"를 함수 이름으로
구별할 수 없어져 검증 가능성이 떨어집니다.

**다만 v3에서도 `collect`라는 이름만으로 「수수료다」가 증명되지는 않습니다.**
`decreaseLiquidity`가 적립한 원금도 같은 `tokensOwed`에 섞이기 때문입니다.

| batch 구성 | 무엇을 회수한 것인가 |
|---|---|
| `collect` 단독 | **수수료만** |
| `decreaseLiquidity` + `collect` | **원금 포함** |

구별의 근거는 **예약된 batch에 `decreaseLiquidity`가 들어 있는지**이고,
그것은 calldata에 그대로 보입니다. v4 대비 우위는 「이름」이 아니라
**두 동작이 분리돼 있어 batch만 보면 판별된다**는 점입니다.

> LP 관련 작업을 타임락으로 예약할 때, **같은 calldata를 두 번 예약하려면 salt를 바꿔야 합니다.**
> `collect()`처럼 인자가 매번 같은 호출에서 반드시 걸립니다.
> `test_SameOperationCannotBeScheduledTwice`가 이 함정을 고정해뒀습니다.

---

### 살아 있는 예약이 있는지 확인하기

손으로 대장을 만들지 마세요. 체인이 이미 갖고 있고, 그쪽은 위조가 불가능합니다.

```bash
# 1) 예약된 적 있는 id 전부
cast logs --address <TIMELOCK> \
  "CallScheduled(bytes32,uint256,address,uint256,bytes,bytes32,uint256)" --rpc-url base

# 2) 각 id가 아직 살아 있는지
cast call <TIMELOCK> "getTimestamp(bytes32)(uint256)" <ID> --rpc-url base
#    0 = 예약된 적 없음
#    1 = 실행 완료
#    그 외 = 아직 살아 있음  ← 이것만 확인하면 됩니다
```

**`getTimestamp(id) > 1` 이면 살아 있는 예약입니다.**
홀더도 같은 명령으로 확인할 수 있습니다.

---

## 하지 말아야 할 것

- ❌ `Token.sol` / `FounderVesting.sol`에 기능 추가 — 무엇이든. 제안도 하지 마세요 (D-001)
- ❌ 장기 재고·타임락을 감싸는 래퍼 컨트랙트 — OpenZeppelin을 그대로 쓰는 게 요점입니다
- ❌ 베스팅 수령 주소를 지갑으로 되돌리기 (D-015)
- ❌ 타임락 `executors`에서 `address(0)` 제거 — 키 분실 시 브릭됩니다
- ❌ 타임락 `proposers`에 EOA 넣기 — 키 하나가 예약과 취소를 독점하면 영구 동결입니다 (D-007, D-019)
- ❌ `SAFE_ADDRESS` 미설정 시 폴백 되살리기 — 조용히 배포 지갑이 운영자가 됩니다
- ❌ 거부권(veto) 키 재론 — D-019에서 기각했고 배포 전에만 넣을 수 있습니다
- ❌ `Deploy.s.sol` 상수를 `internal`로 되돌리기 — 드리프트 탐지가 사라집니다
- ❌ 조건부 소각 로직 — 커스텀 코드가 되고 감사 대상이 생깁니다
- ❌ 에어드랍·머클 배포 컨트랙트 (D-005)
- ❌ 스테이킹·보상 컨트랙트 — `mint`가 없어 재고에서 빼야 하고, 그건 유사수신 경로입니다
- ❌ 2면 유동성 풀 — 상대 자산은 USDC, 단면만 (D-011)
- ❌ 종료 조건 8주를 **늘리는** 방향 수정, 배포 한도 8%를 **늘리는** 방향 수정 (D-013)
- ❌ `docs/UTILITY.md`의 조건을 **낮추거나** 기각 목록에서 항목 빼기 (D-017, D-013)
- ❌ `PRIVATE_KEY` / `.env` 읽기, 출력, 요청
- ❌ 예약해두고 방치 — 실행 안 할 거면 `cancel()`. 실패한 실행은 취소가 아닙니다 (D-024)
- ❌ 사용자 대신 `--broadcast` 실행

---

## 아직 정해지지 않은 것

| 항목 | 언제 |
|---|---|
| ~~토큰 이름·심볼~~ | **D-027로 확정** — `Yumi` / `YUMI` |
| 풀 생성 후 **실제 가격 확정** | 틱 스페이싱 때문에 목표가와 −0.6~1.2% 어긋납니다. 실제 tick에서 역산한 값을 공개 숫자로 확정하고 문서를 고칠 것 (아래 ①) |
| 종료 시 재고 처리 | **D-021로 확정** — 잠긴 물량은 베스팅 소유권을 소각 주소로 |
| 유틸리티 | **조건 셋 충족 이후.** [`UTILITY.md`](UTILITY.md) 참조 (D-017) |
| 영문 계정 분리 | **영어 댓글·문의가 반복적으로 올 때.** 그 전에는 만들지 않습니다 (D-029) |

이름을 서두르지 마세요. 배포하면 영원히 못 바꿉니다.
